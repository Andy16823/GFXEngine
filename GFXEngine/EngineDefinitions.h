#pragma once

namespace GFXEngine {
	namespace Defintions {

		enum PipelineType {
			PRESENT_PIPELINE = 'P',
			GEOMETRY_PIPELINE = 'G',
			INSTANCED_GEOMETRY_PIPELINE = 'I',
			ENVIRONMENT_PIPELINE = 'E',
			DEBUG_PIPELINE = 'D'
		};

	}
}